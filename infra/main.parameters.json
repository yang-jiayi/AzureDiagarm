{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "azureOpenAiEndpoint": {
      "value": "${AZURE_OPENAI_ENDPOINT}"
    },
    "azureOpenAiResourceId": {
      "value": "${AZURE_OPENAI_RESOURCE_ID}"
    },
    "azureOpenAiApiKey": {
      "value": "${AZURE_OPENAI_API_KEY}"
    },
    "feedbackEmailEndpoint": {
      "value": "${FEEDBACK_EMAIL_ENDPOINT}"
    },
    "feedbackEmailSender": {
      "value": "${FEEDBACK_EMAIL_SENDER}"
    },
    "feedbackEmailRecipient": {
      "value": "${FEEDBACK_EMAIL_RECIPIENT}"
    },
    "azureTablesEndpoint": {
      "value": "${AZURE_TABLES_ENDPOINT}"
    },
    "azureTablesFeedbackTable": {
      "value": "${AZURE_TABLES_FEEDBACK_TABLE}"
    },
    "openAiDeploymentGpt51": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_NAME}"
    },
    "openAiDeploymentGpt52": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_GPT52}"
    },
    "openAiDeploymentGpt52Codex": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_GPT52CODEX}"
    },
    "openAiDeploymentGpt53Codex": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_GPT53CODEX}"
    },
    "openAiDeploymentGpt54": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_GPT54}"
    },
    "openAiDeploymentGpt54Mini": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_GPT54MINI}"
    },
    "openAiDeploymentDeepSeek": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_DEEPSEEK}"
    },
    "openAiDeploymentGrokFast": {
      "value": "${AZURE_OPENAI_DEPLOYMENT_GROK4FAST}"
    },
    "deploySpeech": {
      "value": true
    },
    "speechRegion": {
      "value": "${AZURE_SPEECH_REGION}"
    },
    "deployCosmos": {
      "value": false
    },
    "mcpAuthToken": {
      "value": "${MCP_AUTH_TOKEN}"
    }
  }
}
